% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filterDeploymentData.R
\name{filterDeploymentData}
\alias{filterDeploymentData}
\title{Identify and extract deployment periods from archival tag data}
\usage{
filterDeploymentData(
  data,
  id.col = "ID",
  datetime.col = "datetime",
  depth.col = "depth",
  custom.deployment.times = NULL,
  depth.threshold = 3.5,
  variance.threshold = 6,
  max.changepoints = 6,
  temp.col = "temp",
  use.temperature = TRUE,
  min.deployment.hours = 0.25,
  plot = FALSE,
  plot.file = NULL,
  exclusions.file = NULL,
  plot.metrics = NULL,
  plot.metrics.labels = NULL,
  return.data = TRUE,
  output.dir = NULL,
  output.suffix = NULL,
  compress = TRUE,
  verbose = "detailed"
)
}
\arguments{
\item{data}{A tag dataset, a list of tag datasets, a data frame containing multiple individuals
identified by \code{id.col}, or a character vector of \code{.rds} file paths. When file paths are supplied,
deployments are processed sequentially, allowing large collections to be filtered without loading
them all into memory.}

\item{id.col}{Column identifying individuals (default \code{"ID"}).}

\item{datetime.col}{Column containing timestamps (default \code{"datetime"}). Must be of class \code{POSIXct}.}

\item{depth.col}{Column containing depth measurements (default \code{"depth"}). Required for automatic
detection, and for estimating a missing boundary when custom deployment times are only partially
specified.}

\item{custom.deployment.times}{Optional data frame or data table of known deployment boundaries,
containing the column named by \code{id.col} together with \code{start} and \code{end} columns of class \code{POSIXct}.
Either boundary may be given as \code{NA} and estimated automatically. Each individual may have at most
one entry. Default \code{NULL}.}

\item{depth.threshold}{Minimum mean depth, in metres, for a segment to be classified as part of the
deployment period (default \code{3.5}). Ignored where both custom boundaries are supplied.}

\item{variance.threshold}{Minimum depth variance for a segment to be classified as part of the
deployment period; less variable segments are treated as spurious (default \code{6}). Ignored where both
custom boundaries are supplied.}

\item{max.changepoints}{Maximum number of change points considered during automatic detection
(default \code{6}). Passed to \code{\link[changepoint:cpt.meanvar]{changepoint::cpt.meanvar()}}. Ignored where both custom boundaries are
supplied.}

\item{temp.col}{Temperature column used to corroborate automatic detection (default \code{"temp"}).
Corroboration is skipped without error where the column is absent.}

\item{use.temperature}{Whether temperature may be used as a secondary signal during fully automatic
detection (default \code{TRUE}). Temperature can extend or rescue a depth-based window but never shorten
one. Ignored where custom boundaries are supplied.}

\item{min.deployment.hours}{Minimum duration, in hours, for an automatically detected deployment to
be retained (default \code{0.25}, i.e. 15 minutes). Shorter windows are excluded. The criterion applies
only to fully automatic detection; see Details.}

\item{plot}{Whether to draw diagnostic plots to the active graphics device (default \code{FALSE}).
Plotting long, high-resolution series to screen is slow; for a batch, prefer \code{plot.file}.}

\item{plot.file}{Path to a multi-page PDF in which diagnostic plots are saved, one page per
individual, or \code{NULL} (default). The parent directory must already exist, and the path must end in
\code{.pdf}. Independent of \code{plot}: set either, or both.}

\item{exclusions.file}{Path to an \code{.rds} file in which excluded deployments are recorded, or \code{NULL}
(default). The exclusions table is always attached to the result as the \code{"nautilus.exclusions"}
attribute; this argument only decides whether it is also written to disk. Write it when the next
step reads \code{.rds} files from \code{output.dir} rather than the returned objects, since an attribute does
not survive that hand-off, and pass the file to \code{\link[=summarizeTagData]{summarizeTagData()}} so that an excluded deployment
reports its window rather than an empty row. Do not place it inside \code{output.dir}.}

\item{plot.metrics}{Optional character vector of length two naming additional columns to display
alongside depth in the diagnostic plots. Defaults to \code{c("temp", "ax")} where plots are requested.
These are used for visualisation only and are not required for detection; a column absent from a
given dataset is omitted from that panel rather than raising an error.}

\item{plot.metrics.labels}{Optional character vector of length two giving axis labels for
\code{plot.metrics}. If \code{NULL} (default), labels are generated automatically for recognised
\pkg{nautilus} sensor channels, falling back to the column name. Ignored unless plots are drawn.}

\item{return.data}{Whether to return the filtered datasets in memory (default \code{TRUE}). When \code{FALSE},
the function returns the paths of the \code{.rds} files written to \code{output.dir}, which feed directly
into the next step's \code{data} argument; this requires \code{output.dir} to be specified.}

\item{output.dir}{An existing directory in which to save one filtered \verb{<id>.rds} file per deployment.
Supplying a directory is what triggers saving; \code{NULL} (default) writes nothing.}

\item{output.suffix}{Optional string appended to each saved file name, before \code{.rds}, to label a
processing run or avoid overwriting an earlier one. Only used when \code{output.dir} is specified.}

\item{compress}{Compression used when saving \code{.rds} files: \code{TRUE} (default, gzip), \code{FALSE}, or one of
\code{"gzip"}, \code{"bzip2"} or \code{"xz"}. Only used when \code{output.dir} is specified. See \code{\link[base:saveRDS]{base::saveRDS()}}.}

\item{verbose}{How much detail to print: \code{0}/\code{"quiet"}, \code{1}/\code{"normal"} (header, one line per
individual and a final summary), or \code{2}/\code{"detailed"} (default), which adds per-deployment
diagnostics.}
}
\value{
When \code{return.data = TRUE}, a named list of filtered \code{data.table} objects, one per
successfully processed deployment and keyed by identifier. When \code{return.data = FALSE}, a character
vector of the paths to the filtered \code{.rds} files, returned invisibly and ready to pass to the next
step's \code{data} argument.

Either way, excluded deployments are recorded in the \code{"nautilus.exclusions"} attribute of the
result. Files are written whenever \code{output.dir} is specified, regardless of the value of
\code{return.data}. Diagnostic plots are produced as a side effect and are not part of the return value.
}
\description{
Identifies the period during which an archival tag was deployed on an animal and trims each record
to that interval. Deployment periods can be identified automatically from changes in the depth
record, or specified manually through \code{custom.deployment.times}.

Automatic detection uses change-point analysis of depth measurements to identify transitions between
the pre-deployment, on-animal and post-deployment periods. Temperature can optionally be used as a
secondary signal where depth alone is insufficient. Records that do not meet the minimum deployment
duration are excluded.

The function is intended to be applied early in the \pkg{nautilus} workflow, before temporal
regularisation and subsequent analyses, so that measurements recorded outside the deployment are
removed before downstream processing. Left in place, they bias any quantity computed from the
record, including surface time, activity budgets, temperature ranges and the baseline against which
dives are detected.
}
\details{
\subsection{Automatic deployment detection}{

The deployment period is identified from changes in the statistical properties of the depth record.
Depth measured before deployment and after recovery is typically near-constant, whereas depth
measured while the tag is on a diving animal varies considerably. Detection therefore looks for
changes in mean and variance together, using binary segmentation via \code{\link[changepoint:cpt.meanvar]{changepoint::cpt.meanvar()}},
and retains the interval between the pre- and post-deployment phases.

Candidate segments are evaluated against \code{depth.threshold} and \code{variance.threshold}. Appropriate
values depend on the animal's behaviour and on how noisy the depth sensor is at the surface, and are
left to the user to set.

For records sampled above 1 Hz, depth is averaged to 1 Hz for change-point detection only: a
deployment-scale transition is not resolved any better at higher rates, and the reduction keeps the
search tractable on multi-day records. The returned data retain their original resolution and are
trimmed using the detected boundaries.
}

\subsection{Temperature corroboration}{

Where \code{use.temperature = TRUE} and a suitable temperature column is available, temperature acts as a
secondary signal in the fully automatic procedure. It can extend or rescue a depth-based window where
a sufficiently clear and sustained difference exists between the out-of-water and in-water
temperature regimes, which can recover a deployment that remained too shallow for the depth criterion
to detect. Temperature never overrides or shortens a window identified from depth, and a flat or
uninformative temperature record leaves the depth result unchanged.

Accelerometer, gyroscope and magnetometer measurements are deliberately not used for detection.
Handling, attachment and recovery generate substantial motion that would confound a motion-based
detector.
}

\subsection{Custom deployment times}{

Known boundaries can be supplied through \code{custom.deployment.times}, matched to individuals using
\code{id.col}. This is recommended where the depth record contains no clear transition at one or both
ends: where the tag was attached to an animal already submerged, where the animal remained near the
surface throughout, or where the tag was recovered in the water and kept recording.

Both boundaries may be specified, or either one individually. Where only one is supplied, the other
is estimated using the same depth-based procedure as automatic detection, and \code{depth.threshold},
\code{variance.threshold} and \code{max.changepoints} apply to that estimate. Where both are supplied,
detection is not performed and those parameters are ignored. The two approaches can be mixed within a
single call, giving explicit times for awkward records and letting the remainder be detected.
}

\subsection{Minimum deployment duration}{

Detected deployment periods shorter than \code{min.deployment.hours} are excluded, which removes transient
depth excursions, sensor spikes and brief test dives that would otherwise be identified as
deployments.

\strong{The criterion applies only to fully automatic detection.} It is not applied where either boundary
was supplied through \code{custom.deployment.times}, including the partially specified case in which one
boundary was estimated from the depth record. A supplied boundary is taken as given.

Deployments rejected as too short are recorded separately from records in which no deployment period
could be identified at all: the two mean different things and call for different responses. A
rejected record is still drawn for diagnostic review, with the rejected window marked, because
duration alone cannot distinguish a genuinely short deployment from a sensor artefact. It is never
trimmed, returned or saved.
}

\subsection{Diagnostic plots}{

Diagnostic plots support visual assessment of the detected boundaries, which is the practical way to
confirm a boundary before committing to it. Each panel shows the depth record together with up to two
further variables selected through \code{plot.metrics}, and marks the detected or custom boundaries
alongside the relevant deployment metadata.
}

\subsection{Excluded deployments}{

Every deployment that leaves without data is recorded in an exclusions table giving its identifier,
the rule that set it aside and, where one was found and then rejected as too short, the window that
was detected. The table is attached to the result as the \code{"nautilus.exclusions"} attribute, and is
additionally written to disk where \code{exclusions.file} is supplied.
}

\subsection{Processing history}{

For each successfully filtered deployment, the returned data are restricted to the interval between
the selected start and end times. Those boundaries are recorded in the deployment metadata and
appended to the processing history, together with whether they were detected or supplied.
}
}
\examples{
\dontrun{
imported <- importTagData(folders, metadata = meta)

# Identify and extract deployment periods automatically, reviewing the result.
deployed <- filterDeploymentData(imported,
                                 min.deployment.hours = 0.25,
                                 plot.file = "./qc/deployment_windows.pdf")

# Supply known boundaries for individuals whose depth record has no clear transition.
deployment_times <- data.frame(
  ID    = c("TAG_01", "TAG_02"),
  start = as.POSIXct(c("2023-06-01 10:00:00", "2023-06-02 09:30:00"), tz = "UTC"),
  end   = as.POSIXct(c("2023-06-02 14:00:00", "2023-06-03 16:00:00"), tz = "UTC"))

deployed <- filterDeploymentData(imported, custom.deployment.times = deployment_times)

# A batch too large for memory: read paths, write filtered files, keep the exclusions table.
filterDeploymentData(list.files("./imported", full.names = TRUE),
                     exclusions.file = "./qc/exclusions.rds",
                     return.data = FALSE, output.dir = "./deployed")
}
}
\seealso{
\code{\link[=importTagData]{importTagData()}} for importing and standardising archival tag data;
\code{\link[=regularizeTimeSeries]{regularizeTimeSeries()}} for placing the trimmed record on a regular time grid;
\code{\link[=processTagData]{processTagData()}} for the movement and orientation processing that follows;
\code{\link[=summarizeTagData]{summarizeTagData()}} for a per-deployment overview that can read \code{exclusions.file};
\code{\link[changepoint:cpt.meanvar]{changepoint::cpt.meanvar()}} for the underlying change-point analysis.
}
