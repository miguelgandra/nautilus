% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processingSummary.R
\name{processingSummary}
\alias{processingSummary}
\title{Per-deployment processing summary from processTagData() provenance}
\usage{
processingSummary(data, id.col = "ID")
}
\arguments{
\item{data}{Processed nautilus tag(s): a named list of tag objects, a single tag object, a character
vector of \code{.rds} file paths, or a directory containing processed \code{.rds} files.}

\item{id.col}{Name of the ID column used only when \code{data} is a single combined data.frame that must be
split by deployment. Default \code{"ID"}.}
}
\value{
A \code{nautilus_processing_summary} (a \code{data.frame} subclass), one row per deployment, with typed
columns: \code{id}, \code{tag_model}, \code{algorithm} (orientation estimator), \code{median_pitch} / \code{median_roll}
(deg), \code{pitch_offset} / \code{pitch_r2} / \code{roll_offset} (the applied mounting-offset corrections),
\code{coreg_corr} (accelerometer<->gyroscope co-registration correlation from \code{\link{applyAxisMapping}};
NA without a gyroscope), \code{hard_iron_uT}, \code{heading_conf} (stored magnetometer heading confidence
\code{"high"}/\code{"medium"}/\code{"low"} from \code{\link{calibrateMagnetometer}}; NA if none stored), \code{mag_radcv}
(calibrated-radius CV) and \code{mag_dip_resid} (IGRF dip residual, deg), \code{declination} (deg),
\code{drift_status}, \code{drift_offset_m} (max |offset| applied), \code{drift_residual_m}, \code{drift_anchors},
\code{hz_in} / \code{hz_out} (sampling rates), \code{n_in} / \code{n_out} (row counts), and \code{flag} (comma-separated
orientation anomalies: \code{"pitch"}, \code{"roll"}, or \code{""}). A tag not processed by
\code{\link{processTagData}} yields a row of \code{NA}s.
}
\description{
Projects the provenance that \code{\link{processTagData}} records into each object - its
\code{processTagData} and \code{depth_drift} audit-trail entries - into a tidy, one-row-per-deployment table.
This is the scannable cross-deployment view of \emph{what the pipeline did} (mounting-offset corrections,
magnetometer calibration, depth-drift outcome, downsampling), complementing \code{\link{summarizeTagData}},
which summarises \emph{what the animal did}.

Because it reads stored metadata rather than recomputing anything, it works on the in-memory objects
returned by \code{\link{processTagData}} OR on the saved \code{.rds} files at any later time, and never
re-runs processing. It is therefore independent of \code{processTagData}'s \code{return.data} argument.
}
\examples{
\dontrun{
processed <- processTagData(tags)
processingSummary(processed)                       # from the in-memory list
processingSummary("./data interim/processed")      # or from the saved files, any time later
}
}
\seealso{
\code{\link{processTagData}}, \code{\link{summarizeTagData}}, \code{\link{processingHistory}}
}
