% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nautilus-class.R
\name{updateBiometrics}
\alias{updateBiometrics}
\title{Refresh biological traits on already-processed data}
\usage{
updateBiometrics(
  data,
  metadata,
  columns = metadataColumns(),
  id.col = "ID",
  return.data = TRUE,
  output.dir = NULL,
  output.suffix = NULL,
  compress = TRUE,
  verbose = "detailed"
)
}
\arguments{
\item{data}{A \code{nautilus_tag} / data.frame, a (named) list of them, or a character vector of \code{.rds}
paths (the output of any processing step).}

\item{metadata}{The deployment-metadata table (a data.frame or a \code{nautilus_deployments} object),
one row per deployment, holding the trait columns and the id column.}

\item{columns}{A \code{\link{metadataColumns}} object naming the id column (\code{id}) and the trait
columns (\code{traits}). Traits not listed here are left untouched.}

\item{id.col}{Character. Name of the ID column used to match each dataset to its metadata row.
Default \code{"ID"}.}

\item{return.data}{Logical. Return the processed data in memory (default \code{TRUE}). When \code{FALSE}, the
function instead returns the paths of the \code{.rds} files it wrote, which feed directly into the next
step's \code{data} argument -- so a large fleet can be processed without ever holding it all in memory.
\code{return.data = FALSE} therefore requires an \code{output.dir}.}

\item{output.dir}{Character. Directory in which to write one \verb{<id>.rds} file per deployment. Providing
a directory is what triggers saving; \code{NULL} (default) writes nothing. The directory must already exist.}

\item{output.suffix}{Character. Optional suffix appended to each saved file name (before \code{.rds}), e.g.
to tag a processing run or avoid clashes. Only used when \code{output.dir} is set. Default \code{NULL}.}

\item{compress}{Compression for the saved \code{.rds} files (only used when \code{output.dir} is set): \code{TRUE}
(default, gzip), \code{FALSE}, or one of \code{"gzip"}/\code{"bzip2"}/\code{"xz"}. See \code{\link[base]{saveRDS}}.}

\item{verbose}{Verbosity: \code{FALSE}/\code{0}/"quiet", \code{TRUE}/\code{1}/"normal", or \code{2}/"detailed" (default).}
}
\value{
If \code{return.data = TRUE}, a named list of updated objects; if \code{return.data = FALSE}, a character
vector of the written \code{.rds} file paths.
}
\description{
Re-stamps the biological traits (\code{tagMetadata(x)$biometrics}) of already-imported / processed data
from a (corrected) deployment-metadata table, WITHOUT re-reading the raw sensor files. Traits are
normally captured once at import (via \code{metadataColumns(traits = ...)}), but if a value is later fixed
or a new trait added, this propagates it to the processed objects cheaply - so the self-contained
objects stay the single source of truth while remaining correctable.
}
\examples{
\dontrun{
# correct a trait in the deployment table, then re-stamp it onto processed data
metadata <- data.frame(ID = c("shark01", "shark02"),
                          sex = c("F", "M"), length_cm = c(612, 548))
cols <- metadataColumns(id = "ID", traits = c("sex", "length_cm"))
tags <- updateBiometrics(processed, metadata, columns = cols, id.col = "ID")
}
}
\seealso{
\code{\link{metadataColumns}}, \code{\link{importTagData}}, \code{\link{tagMetadata}}
}
