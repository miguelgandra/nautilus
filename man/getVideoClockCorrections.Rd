% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getVideoMetadata.R
\name{getVideoClockCorrections}
\alias{getVideoClockCorrections}
\title{Derive video-clock corrections from imported tag metadata}
\usage{
getVideoClockCorrections(data)
}
\arguments{
\item{data}{Imported sensor data in any standard pipeline form: a \code{nautilus_tag}, a named list of tag
objects, or paths to saved \code{.rds} objects.}
}
\value{
A data frame with one row per suggested deployment correction and columns \code{ID},
\code{clock_correction_s}, \code{clock_correction_source}, \code{device_utc_offset_h}, and
\code{logging_utc_offset_h}. A deployment needing no correction is omitted.
}
\description{
Builds a small, reviewable correction table from the device and sensor-logging clock metadata retained
by \code{\link[=importTagData]{importTagData()}}. It does not modify either the sensor data or any video timestamps. Pass the result
explicitly to the \code{clock.corrections} argument of \code{\link[=getVideoMetadata]{getVideoMetadata()}} to apply it.
}
\details{
A correction is derived only for the unambiguous CATS sidecar case: \verb{[logging]} explicitly states that
the sensor stream is UTC, the sensor object was imported in a fixed UTC-equivalent time zone, and
\verb{[device] utc_offset} is finite and non-zero. The device clock is converted to UTC by adding
\code{-utc_offset * 3600} seconds. Thus a device offset of \code{-1} produces a video-clock correction of \code{+3600}
seconds.

A non-zero device offset without an explicit UTC logging declaration is reported but omitted. The
function deliberately does not infer corrections from ambiguous or non-UTC logging metadata.
}
\examples{
\dontrun{
corrections <- getVideoClockCorrections(imported_tags)
corrections

video_metadata <- getVideoMetadata("./videos", clock.corrections = corrections)
}
}
\seealso{
\code{\link[=getVideoMetadata]{getVideoMetadata()}} for applying the returned corrections while extracting video timing;
\code{\link[=tagMetadata]{tagMetadata()}} for inspecting the underlying sidecar provenance.
}
