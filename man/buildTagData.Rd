% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buildTagData.R
\name{buildTagData}
\alias{buildTagData}
\title{Build a tag object from data already in R}
\usage{
buildTagData(
  data,
  id = NULL,
  datetime.col = "datetime",
  start = NULL,
  sampling.rate = NULL,
  metadata = NULL,
  traits = NULL,
  sensor.mapping = NULL,
  timezone = "UTC",
  required.sensors = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame or data.table with one row per sample: a timestamp column - or \code{start} and
\code{sampling.rate} instead - and one or more sensor columns.}

\item{id}{The deployment identifier. \code{NULL} (default) takes it from an \code{ID} column in \code{data}, or
from \code{metadata}. An error is raised if none is available, or if \code{data} mixes several animals.}

\item{datetime.col}{Which column holds the timestamps (default \code{"datetime"}). Set it when your
table names that column something else; it is renamed on output so the rest of the pipeline sees
a consistent name.}

\item{start, sampling.rate}{For loggers that record no clock: the time of the first sample and the
recording rate in Hz, from which the timestamps are generated. Little Leonardo tags are the
common case. Ignored when a timestamp column is present.}

\item{metadata}{Optional deployment metadata: a single-row data frame, a \code{nautilus_deployments}
object, or a named list, keyed by the role names from \code{\link[=metadataColumns]{metadataColumns()}}.}

\item{traits}{Which columns of \code{metadata} to carry through as biometric traits, for example length,
mass or sex. These are stored with the tag and travel with it, but are not used by any analysis
step.}

\item{sensor.mapping}{How your column names map onto the canonical sensor names, for example
\code{c(ax = "X", ay = "Y", az = "Z")}. Needed only when the columns are not already canonically named.}

\item{timezone}{The time zone the timestamps are recorded in (default \code{"UTC"}). This records what
the timestamps mean; it does not shift them.}

\item{required.sensors}{Which sensor channels a tag must carry to be built. \code{NULL} (default)
requires only a timestamp and at least one recognised channel. Name the channels your analysis
depends on to have an incomplete tag rejected here rather than later.}

\item{verbose}{How much detail to print: \code{0}/\code{"quiet"}, \code{1}/\code{"normal"}, or \code{2}/\code{"detailed"}.}
}
\value{
A validated \code{nautilus_tag}: a data.table carrying the sensor data together with the
deployment metadata and a processing record, ready for \code{\link[=processTagData]{processTagData()}}.
}
\description{
Not every deployment arrives as a file nautilus can read. The logger may use an export format that
is not yet supported, the data may have been through another analysis tool, a collaborator may send
a processed table, or the record may be simulated for testing a method.

\code{buildTagData()} assembles sensor data you already hold in R into a tag object identical in
structure to one \code{\link[=importTagData]{importTagData()}} reads from disk, so it flows through the rest of the pipeline
unchanged.

This is a constructor, not a processing step: it applies no axis rotation, no calibration and no
unit conversion. Supply the channels in the tag's own recorded frame, with acceleration in g.
Reorienting the axes is a later, deliberate step - see \code{\link[=applyAxisMapping]{applyAxisMapping()}}.
}
\details{
\subsection{Sensor columns}{

One row per sample. Channels should use the canonical names \code{ax}, \code{ay}, \code{az}, \code{gx}, \code{gy}, \code{gz},
\code{mx}, \code{my}, \code{mz}, \code{depth}, \code{temp}, \code{paddle_speed} and \code{paddle_freq}; whichever are present are
recorded in the tag's metadata. Columns under other names can be renamed on the way in with
\code{sensor.mapping}. At least one sensor channel is required.
}

\subsection{Timestamps}{

Either supply a POSIXct column naming the time of each sample, or - for loggers that record only a
start time and a fixed rate - supply \code{start} and \code{sampling.rate}, and the timestamps are generated
as \code{start + (0:(n - 1)) / sampling.rate}. Timestamps are taken as given in \code{timezone} and are never
silently reinterpreted.
}

\subsection{Deployment metadata}{

Optional. Metadata is read using the role names from \code{\link[=metadataColumns]{metadataColumns()}} - where and when the tag
was deployed and recovered, the tag model and type, the attachment site, and so on. A single-row
object from \code{\link[=checkDeploymentMetadata]{checkDeploymentMetadata()}} can be passed directly, since its columns already use those
names. Columns named in \code{traits} are carried through unchanged as biometric traits, for example
length or sex. Anything not supplied stays missing; none of it is needed to run an accelerometer
analysis.
}

\subsection{Tags without a magnetometer or gyroscope}{

A tag carrying only accelerometer channels and depth is fully usable. \code{\link[=processTagData]{processTagData()}} derives
dynamic body acceleration, the surge, sway and heave components and pitch and roll from
acceleration alone, leaving heading missing, and \code{\link[=checkTagMapping]{checkTagMapping()}} can still resolve the
tag-to-body axes from gravity and diving behaviour.

Note that \code{\link[=processTagData]{processTagData()}} does require a \code{depth} channel, which it uses for vertical velocity and
for the mounting-pitch correction. \code{buildTagData()} itself does not, so a depth-less tag can be
built for use with the functions that do not need it.
}
}
\examples{
# a minimal accelerometer-only tag from an in-memory data frame
n  <- 6000
df <- data.frame(
  datetime = as.POSIXct("2025-01-01", tz = "UTC") + (seq_len(n) - 1) / 20,
  ax = rnorm(n), ay = rnorm(n), az = 1 + rnorm(n), depth = abs(rnorm(n, 5)))
tag <- buildTagData(df, id = "DEMO01")

# loggers with no timestamp column: synthesise from a start time + rate
raw <- data.frame(ax = rnorm(n), ay = rnorm(n), az = 1 + rnorm(n))
tag <- buildTagData(raw, id = "DEMO02",
                    start = as.POSIXct("2025-01-01 12:00:00", tz = "UTC"),
                    sampling.rate = 20)
}
\seealso{
\code{\link[=importTagData]{importTagData()}} to read tags from disk; \code{\link[=metadataColumns]{metadataColumns()}} and
\code{\link[=checkDeploymentMetadata]{checkDeploymentMetadata()}} for the metadata; \code{\link[=applyAxisMapping]{applyAxisMapping()}} for the axis frame;
\code{\link[=processTagData]{processTagData()}} for the next step.
}
