% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculateTailBeats.R
\name{calculateTailBeats}
\alias{calculateTailBeats}
\title{Estimate tail-beat frequency and oscillation amplitude from tag motion data}
\usage{
calculateTailBeats(
  data,
  method = c("peaks", "wavelet"),
  id.col = "ID",
  datetime.col = "datetime",
  motion.col = "sway",
  min.freq.Hz = 0.1,
  max.freq.Hz = 3,
  bandpass.filter = TRUE,
  filter.low.freq = NULL,
  filter.high.freq = NULL,
  filter.order = 4,
  min.amplitude = NULL,
  smooth.window = 10,
  max.interp.gap = 10,
  ridge.prominence = 2,
  min.periodicity = 0.15,
  plot = FALSE,
  plot.file = NULL,
  plot.wavelet = TRUE,
  plot.diagnostic = TRUE,
  return.data = TRUE,
  output.dir = NULL,
  output.suffix = NULL,
  compress = TRUE,
  verbose = "detailed"
)
}
\arguments{
\item{data}{A \code{nautilus_tag} object, a list of tag datasets, a data frame containing multiple
individuals identified by \code{id.col}, or a character vector of \code{.rds} file paths. File paths are
processed sequentially, allowing large collections to be analysed without loading them all into
memory. The output of \code{\link[=processTagData]{processTagData()}} is recommended.}

\item{method}{One or both estimation methods, \code{"peaks"} and \code{"wavelet"} (default both). Where both
are given, the first is the primary method and the second a cross-check.}

\item{id.col}{Column identifying individuals (default \code{"ID"}).}

\item{datetime.col}{Column containing timestamps (default \code{"datetime"}).}

\item{motion.col}{One or more candidate motion channels from which the periodic movement is estimated
(default \code{"sway"}). Where several are supplied, the most suitable is selected separately for each
deployment. See Details.}

\item{min.freq.Hz, max.freq.Hz}{Lower and upper limits of the frequency range analysed, in Hz
(defaults \code{0.1} and \code{3}). Choose them from the locomotor frequencies expected of the study species.}

\item{bandpass.filter}{Whether the motion signal is band-pass filtered before analysis (default
\code{TRUE}).}

\item{filter.low.freq, filter.high.freq}{Optional cut-off frequencies for the band-pass filter, in Hz.
\code{NULL} (default) derives them from \code{min.freq.Hz} and \code{max.freq.Hz}, set slightly wider so that the
filter does not attenuate the edges of the band being estimated.}

\item{filter.order}{Order of the Butterworth band-pass filter (default \code{4}).}

\item{min.amplitude}{Optional amplitude threshold, in the units of the selected motion channel, above
which a sample is classified as active swimming. \code{NULL} (default) performs no classification. See
Details.}

\item{smooth.window}{Width, in seconds, of the centred moving average applied to the frequency and
amplitude estimates (default \code{10}). \code{0} disables smoothing.}

\item{max.interp.gap}{Longest gap, in seconds, between valid frequency estimates that may be filled
by linear interpolation (default \code{10}). \code{NULL} disables interpolation.}

\item{ridge.prominence}{Minimum prominence of the dominant wavelet ridge relative to the surrounding
spectral background (default \code{2}). Estimates with less support are returned as \code{NA}. Used only by
\code{"wavelet"}.}

\item{min.periodicity}{Minimum autocorrelation-based periodicity required before an estimate is
reported (default \code{0.15}). Estimates below it are returned as \code{NA}. Used only by \code{"peaks"}.}

\item{plot}{Whether to draw diagnostic plots to the active graphics device (default \code{FALSE}).}

\item{plot.file}{Path to a multi-page PDF holding the diagnostic plots, or \code{NULL} (default).}

\item{plot.wavelet}{Whether the wavelet power spectrum is included in the diagnostic plots (default
\code{TRUE}).}

\item{plot.diagnostic}{Whether the time-series diagnostic panels are included (default \code{TRUE}).}

\item{return.data}{Whether to return the processed datasets in memory (default \code{TRUE}). When \code{FALSE},
the function returns the paths of the \code{.rds} files written to \code{output.dir}, which feed directly into
the next step's \code{data} argument; this requires \code{output.dir} to be specified.}

\item{output.dir}{An existing directory in which to save one \code{.rds} file per deployment. Supplying a
directory is what triggers saving; \code{NULL} (default) writes nothing.}

\item{output.suffix}{Optional string appended to each saved file name, before \code{.rds}, to label a
processing run or avoid overwriting an earlier one. Only used when \code{output.dir} is specified.}

\item{compress}{Compression used when saving \code{.rds} files: \code{TRUE} (default, gzip), \code{FALSE}, or one of
\code{"gzip"}, \code{"bzip2"} or \code{"xz"}. See \code{\link[base:saveRDS]{base::saveRDS()}}.}

\item{verbose}{How much detail to print: \code{0}/\code{"quiet"}, \code{1}/\code{"normal"}, or \code{2}/\code{"detailed"}
(default), which adds per-deployment diagnostics.}
}
\value{
When \code{return.data = TRUE}, the input data with the tail-beat columns added. Which columns
appear depends on the methods requested:

\describe{
\item{\code{tbf_hz_peaks}, \code{tbf_hz_wavelet}}{Estimated frequency in Hz, one column per method that
ran and named after it.}
\item{\code{tbf_amplitude_peaks}, \code{tbf_amplitude_wavelet}}{Oscillation amplitude as a peak-to-trough
excursion, in the units of the selected motion channel.}
\item{\code{tbf_swimming}}{Whether the animal was actively swimming, where \code{min.amplitude} was
supplied, and \code{NA} otherwise. Unsuffixed: it derives from the band-passed signal both methods
share.}
\item{\code{tbf_agree}}{Whether the two methods agree within 10\%, where both ran. Also unsuffixed,
being a property of the pair rather than of either one.}
}

There is deliberately no method-agnostic \code{tbf_hz}, so a value's provenance always travels with it;
use \code{\link[=tailBeatColumn]{tailBeatColumn()}} to resolve which column to read without hard-coding a method.

The channel selected, the methods used, median frequency and amplitude, and the percentage of time
classified as swimming are recorded in the deployment's processing history. When
\code{return.data = FALSE}, a character vector of the written \code{.rds} file paths.
}
\description{
Estimates the dominant periodic motion within a user-defined frequency band from archival tag data.
The resulting frequency may represent tail beats, wing beats or another periodic locomotor movement,
depending on the study species, tag placement and selected motion channel.

The frequency can be estimated by peak detection, by a continuous wavelet transform, or by both. When
both are requested their estimates are kept separately and their agreement is reported as an
additional quality-control metric.

The function expects processed data and should normally follow \code{\link[=processTagData]{processTagData()}}. It can analyse one
or more candidate motion channels and, where several are supplied, selects the most suitable channel
separately for each deployment.
}
\details{
\subsection{Estimation methods}{

\describe{
\item{\code{"peaks"}}{Locates successive oscillatory cycles in the band-passed signal, each contributing
a frequency from its peak-to-peak interval and an amplitude from its peak-to-trough excursion.
Estimates rest on individual cycles.}
\item{\code{"wavelet"}}{Follows the dominant frequency through time with a Morlet continuous wavelet
transform (Torrence and Compo 1998). The estimate is continuous rather than cycle-by-cycle, and
is often steadier on noisy or non-stationary signals.}
}

Both can be requested at once. Their frequencies are stored in separate columns, and \code{tbf_agree}
records whether the two fall within 10\% of each other at a given sample.

Read agreement as a certificate rather than an error flag. Where the two methods concur they are
rarely both wrong; where they differ, nothing identifies which to distrust, so \code{FALSE} means
unresolved rather than bad. The certificate has one known blind spot: both methods establish a
dominant periodicity before anything else, so neither can vouch for the other against a harmonic
error, and both may settle on the same harmonic of the true locomotor frequency.
}

\subsection{Selecting the motion channel}{

Where \code{motion.col} names several candidates, the channel is chosen by agreement in dominant frequency
across them, with in-band signal power breaking ties. Coordinated propulsion drives several axes at
one frequency, whereas a non-locomotor artefact is usually confined to one; choosing on raw in-band
power alone would favour the artefact whenever it is the stronger signal. The classic case is the
pendulum swing of a towed tag, which produces a sharp lateral-axis peak a little below the true beat.

Where no two candidates agree, the channel with the strongest in-band signal is used and the estimate
is flagged as possibly reflecting an artefact. Flat or stuck channels are excluded before selection,
so they cannot form a spurious consensus. Where the deployment metadata marks a tag as towed, a
further warning notes that tow-pendulum motion may contaminate the lateral axis whichever channel is
finally used.
}

\subsection{Harmonic ambiguity}{

A fish swimming by lateral undulation beats on a single axis, sway, while surge and heave carry its
second harmonic. A corroborated surge-and-heave pair can therefore sit at twice the true tail-beat
rate.

That spectrum is identical to a genuine wingbeat consensus contaminated by an artefact near half the
beat, so \strong{the ambiguity cannot be resolved from the signal alone}. Rather than guess, and risk
silently halving a correct estimate, the function keeps the channel it selected and warns whenever
another channel carries a comparable peak at about half the chosen frequency, naming it as the likely
fundamental for a single-axis swimmer. Resolving it needs independent information: video, or
species-specific knowledge of locomotor mechanics.
}

\subsection{Frequency band and filtering}{

\code{min.freq.Hz} and \code{max.freq.Hz} bound the frequencies searched. The sampling rate must exceed twice
\code{max.freq.Hz} to satisfy the Nyquist criterion, and a deployment that fails this is reported; in
practice a substantially higher rate is needed before an estimate is reliable.

The band-pass limits default to slightly outside the estimation band, so that the filter does not
attenuate the frequencies being estimated. Wavelet amplitudes are corrected for the filter's
attenuation, so \code{tbf_amplitude_peaks} and \code{tbf_amplitude_wavelet} measure the same quantity and are
directly comparable.
}

\subsection{When an estimate is withheld}{

\code{min.periodicity} withholds a peak-detection estimate where the record shows too little periodic
structure to support one; \code{ridge.prominence} withholds a wavelet estimate where the dominant ridge is
not distinct enough from the spectral background.

Both govern \emph{whether} an estimate is reported, never \emph{which} frequency is reported. Peak detection is
otherwise data-driven and not user-tunable, so the frequency returned does not depend on any
behavioural threshold.
}

\subsection{Oscillation amplitude}{

Amplitude is the peak-to-trough excursion of the periodic motion, in the units of the selected motion
channel, and serves as a proxy for the magnitude of the oscillation.

It should not be read as a direct measure of swimming effort without validation for the species, tag
placement and sensor configuration.
}

\subsection{Swimming classification}{

Detecting a periodic oscillation does not by itself establish that an animal is actively swimming, so
no swimming-or-gliding classification is performed by default and \code{tbf_swimming} is returned as \code{NA}.

Supplying \code{min.amplitude} enables classification against that threshold. It should be calibrated for
the species and tag configuration, ideally against independently validated behavioural observations.
}

\subsection{Smoothing and interpolation}{

\code{smooth.window} applies a centred moving average to the frequency and amplitude series;
\code{max.interp.gap} optionally fills short gaps between valid estimates by linear interpolation.

Interpolated values represent assumed continuity between estimates rather than measurements, so the
gap allowed should stay short relative to the timescale on which locomotor behaviour changes.
}

\subsection{Diagnostics and quality control}{

The diagnostic plots show the band-passed signal with the estimated frequency and amplitude, and
optionally the wavelet power spectrum with the fitted ridge overlaid, which is the practical way to
judge whether the analysis band, the signal quality and the chosen channel are right.

Quality-control findings cover estimates pressed against the edges of the analysis band, disagreement
between methods, uncertain channel selection and possible harmonic ambiguity. Each is raised once per
run, with the affected deployments named inline, rather than once per deployment, so a large batch
neither buries the console nor loses warnings past the limit R keeps.
}
}
\note{
Band-pass filtering requires the \pkg{signal} package; set \code{bandpass.filter = FALSE} to analyse
the unfiltered signal instead.
}
\examples{
\dontrun{
# Estimate tail-beat frequency by peak detection on a single channel.
tag <- calculateTailBeats(processed, method = "peaks", motion.col = "sway",
                          min.freq.Hz = 0.1, max.freq.Hz = 2.5)

# Let the channel be chosen by consensus across three candidates, and cross-check both methods.
tag <- calculateTailBeats(processed, method = c("peaks", "wavelet"),
                          motion.col = c("sway", "surge", "heave"))

# A batch too large to hold in memory: write a diagnostic PDF and pass the paths on.
calculateTailBeats(list.files("./processed", full.names = TRUE),
                   motion.col = c("sway", "surge", "heave"),
                   plot.file = "./qc/tail_beats.pdf",
                   return.data = FALSE, output.dir = "./tailbeats")
}
}
\references{
Torrence C, Compo GP (1998) A practical guide to wavelet analysis. \emph{Bulletin of the American
Meteorological Society} 79:61-78.
\doi{10.1175/1520-0477(1998)079<0061:APGTWA>2.0.CO;2}
}
\seealso{
\code{\link[=processTagData]{processTagData()}} for deriving the motion channels used as input; \code{\link[=tailBeatColumn]{tailBeatColumn()}} for
reading the result without hard-coding a method; \code{\link[=checkTagMapping]{checkTagMapping()}} for confirming the axis frame
the channels are expressed in; \code{\link[=plotDives]{plotDives()}} and \code{\link[=extractFeatures]{extractFeatures()}} for what typically follows.
}
