% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotTheme.R
\name{plotTheme}
\alias{plotTheme}
\title{Visual theme for nautilus plots}
\usage{
plotTheme(
  preset = c("light", "minimal", "classic"),
  panel = NULL,
  grid = NULL,
  ink = NULL,
  axis = NULL,
  subtitle = NULL,
  palette = NULL,
  sequential = NULL,
  day = NULL,
  night = NULL,
  day.border = NULL,
  bar.alpha = NULL,
  bar.border = NULL,
  font.family = NULL,
  cex = NULL
)
}
\arguments{
\item{preset}{Base style: \code{"light"} (default; soft panel, subtle gridlines), \code{"minimal"} (white
panel), or \code{"classic"} (greyscale, print-journal look).}

\item{panel, grid}{Panel background and gridline colours.}

\item{ink, axis, subtitle}{Colours for titles, axis text/ticks, and subtitles.}

\item{palette}{Qualitative palette for groups: a vector of colours, or the name of an
\code{\link[grDevices:hcl.colors]{grDevices::hcl.colors()}} palette (e.g. \code{"viridis"}).}

\item{sequential}{Continuous ramp (>= 2 colours) for a variable mapped to colour -- currently the
\code{color.by} trace in \code{\link[=plotDepthProfiles]{plotDepthProfiles()}}, the \code{color.by} track in \code{\link[=plotTracks]{plotTracks()}}, and the
heatmaps in \code{\link[=plotTimeAtDepth]{plotTimeAtDepth()}}. The default spans blue to dark red through
cyan, green, yellow and orange, which is what makes a temperature gradient legible at a glance
across a page of small multiples; a monochrome ramp collapses that variation into one hue. Note it
is chosen for discriminability rather than for monotonic lightness, so for a figure that must
survive greyscale printing or that is read by value rather than by pattern, pass a perceptually
uniform ramp instead, e.g. \code{sequential = hcl.colors(9, "Viridis")}.}

\item{day, night}{Fill colours for the diel-split day and night bars. The defaults pair a warm day
with a cool night, so the two sides of a mirrored profile read as two categories rather than one
variable at two intensities.}

\item{day.border}{Outline for the day bar. Left \code{NULL} while \code{day} is supplied, it is derived as a
darker shade of \code{day}, so a restyled fill keeps a matching outline; name it to override. The
night and pooled bars always derive their outline from their own fill.}

\item{bar.alpha}{Fill opacity for bars (0-1).}

\item{bar.border}{Bar border colour.}

\item{font.family}{Font family (e.g. \code{""}/\code{"sans"}, \code{"serif"}).}

\item{cex}{Master text-scaling factor.}
}
\value{
A \code{nautilus_theme} object.
}
\description{
A small style object controlling the shared look of the nautilus plotting functions
(\code{\link[=plotTimeAtDepth]{plotTimeAtDepth()}}, \code{\link[=plotDistributions]{plotDistributions()}}, ...). Pass it as their \code{theme} argument. Start
from a \code{preset} and override individual fields as needed - so figures are publication-ready by default
but trivially adapted to your own palette, transparency, or typography without touching plotting code.

\preformatted{
plotTimeAtDepth(data, theme = plotTheme())                       # polished default
plotTimeAtDepth(data, theme = plotTheme("minimal", cex = 1.2))   # a preset, larger text
plotTimeAtDepth(data, theme = plotTheme(day = "#EAF3FB", palette = "viridis"))
}
}
\examples{
# Build a theme object to pass as the `theme` argument of a nautilus plotter
th <- plotTheme("light")
th <- plotTheme("minimal", cex = 1.2)
th <- plotTheme(day = "#EAF3FB", palette = "viridis")
}
\seealso{
\code{\link[=plotTimeAtDepth]{plotTimeAtDepth()}}
}
