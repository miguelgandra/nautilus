% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{paddleFrequencyControl}
\alias{paddleFrequencyControl}
\title{Paddle-wheel frequency estimation settings for processTagData()}
\usage{
paddleFrequencyControl(
  window.size = 5,
  step.size = 1,
  min.freq.Hz = 0.1,
  max.freq.Hz = NULL,
  nyquist.guard = 0.9,
  min.prominence = 20,
  max.interp.gap = 2
)
}
\arguments{
\item{window.size}{Length of each spectral window in seconds (default \code{5}). Longer windows improve
frequency resolution but follow changes in rotation rate more slowly.}

\item{step.size}{Time between successive estimates in seconds (default \code{1}). It must not exceed
\code{window.size}.}

\item{min.freq.Hz}{Lower frequency searched, in hertz (default \code{0.1}). It must be positive.}

\item{max.freq.Hz}{Optional upper frequency searched, in hertz. \code{NULL} (default) uses the Nyquist-safe
limit set by \code{nyquist.guard}; an explicit value can narrow the band but cannot bypass that guard.}

\item{nyquist.guard}{Fraction of the Nyquist frequency retained as a safe analysis band (default
\code{0.9}). It must lie strictly between zero and one. Power whose dominant peak lies above this limit
is treated as a sampling-boundary artefact rather than reassigned to a lower-frequency peak. This
guard rejects boundary peaks but cannot recover frequencies that were already aliased during
recording; the sensor sampling rate must still be high enough for the physical rotation range.}

\item{min.prominence}{Minimum ratio of peak power to median in-band background power (default \code{20}).
A value of zero disables this quality gate, but the interior-peak and Nyquist guards still apply.}

\item{max.interp.gap}{Longest run of rejected estimates, in seconds, that may be filled between two
accepted estimates (default \code{2}). \code{NULL} or zero leaves every rejected window missing. Leading and
trailing gaps are never extrapolated.}
}
\value{
A validated \code{nautilus_paddle_frequency} object for the \code{paddle} argument of
\code{\link[=processTagData]{processTagData()}}.
}
\description{
Groups the settings used by \code{\link[=processTagData]{processTagData()}} to recover paddle-wheel rotation frequency from the
magnetometer. The estimator works in overlapping windows, reports a frequency only when the strongest
in-band component is an interior spectral peak, and requires that peak to stand above the spectral
background. This prevents broadband noise and sampling-boundary artefacts from becoming precise-looking
rotation rates.
}
\examples{
paddleFrequencyControl(max.freq.Hz = 35, min.prominence = 15)
paddleFrequencyControl(max.interp.gap = NULL)  # retain every rejected interval as missing
}
\seealso{
\code{\link[=processTagData]{processTagData()}} for frequency extraction and \code{\link[=calculatePaddleSpeed]{calculatePaddleSpeed()}} for the separate
calibration step that converts frequency to speed.
}
