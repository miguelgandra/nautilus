% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotTimeAtDepth.R
\name{plotTimeAtDepth}
\alias{plotTimeAtDepth}
\title{Plot time-at-depth and time-at-temperature}
\usage{
plotTimeAtDepth(
  data,
  variable = "depth",
  group = NULL,
  diel = FALSE,
  style = c("profile", "heatmap"),
  bin.width = NULL,
  breaks = NULL,
  n.bins = NULL,
  gap.factor = 3,
  order.by = c("id", "input", "median"),
  same.scale = TRUE,
  coordinates = NULL,
  theme = plotTheme(),
  plot = TRUE,
  plot.file = NULL,
  id.col = "ID",
  datetime.col = "datetime",
  verbose = "detailed"
)
}
\arguments{
\item{data}{Processed data: \code{.rds} paths, a single \code{nautilus_tag} / data.frame, or a list of them.}

\item{variable}{Character. \code{"depth"} (default), \code{"temp"}, or \code{c("depth","temp")} for both.}

\item{group}{Grouping factor for faceting: a column name (in the data or the tag metadata, e.g. mapped
at import via \link{metadataColumns}), a named \code{id -> group} vector, or a two-column
\code{data.frame(id, group)}. \code{NULL} (default) pools all individuals into one profile.}

\item{diel}{Logical. Split each bin into night vs day back-to-back bars (needs coordinates; see
\code{coordinates}). Default \code{FALSE}. Ignored by \code{style = "heatmap"}.}

\item{style}{\code{"profile"} (default) or \code{"heatmap"} (a deployment x bin heatmap - compact for many
individuals).}

\item{bin.width, breaks, n.bins}{Bin definition. \code{breaks} gives explicit (possibly non-uniform) edges;
else \code{bin.width} (uniform) or \code{n.bins} pretty bins; else a smart per-variable default (surface-fine
for depth, uniform for temperature).}

\item{gap.factor}{Numeric >= 1. A sample-to-next interval beyond \code{gap.factor} x the modal interval is
treated as a gap and credited only the modal interval. Default 3.}

\item{order.by}{Ordering of groups / heatmap columns: \code{"id"}, \code{"input"}, or \code{"median"}.}

\item{same.scale}{Logical. Give every panel of a variable the same x range, so bar lengths are
comparable across group facets (the comparison the faceting exists to support). Default \code{TRUE}.
\code{FALSE} lets each panel autoscale, which resolves detail in a group whose time budget is much
flatter than the others - at the cost of equal-length bars meaning different values. Depth and
temperature always keep separate scales.}

\item{coordinates}{For \code{diel}: a \code{c(lon, lat)}, a named \code{id -> c(lon,lat)} list, or a
\code{data.frame(id, lon, lat)}. If \code{NULL}, longitude/latitude are read from the data or tag metadata.}

\item{theme}{A \link{plotTheme} object (or a list of overrides) controlling the visual style.}

\item{plot}{Logical. Draw to the active device. Default \code{TRUE}.}

\item{plot.file}{Character. Path to a PDF to draw into. Default \code{NULL}.}

\item{id.col, datetime.col}{Column names for the deployment id and datetime.}

\item{verbose}{Verbosity: \code{FALSE}/\code{0}/"quiet" (silent), \code{TRUE}/\code{1}/"normal" (header + summary), or
\code{2}/"detailed" (default): additionally reports the modal bin per variable and any skipped
deployments, and shows a live progress bar while the tags are read (cli auto-hides it for fast runs).}
}
\value{
Invisibly, a tidy data frame with one row per deployment x variable x diel-phase x bin:
\code{id}, \code{group}, \code{variable}, \code{phase}, \code{bin_min}, \code{bin_max}, \code{bin_mid}, \code{hours}, \code{pct} (percent of that
deployment/phase's time; sums to 100 per \code{id}/\code{variable}/\code{phase}).
}
\description{
Shows HOW a cohort allocates time across the depth (or temperature) envelope. The variable is binned
and each bin's share of time is drawn as a horizontal \strong{profile} - the ecological complement to the
scalar min / mean / max of \link{summarizeTagData} and the raw traces of \link{plotDepthProfiles}.

One composable geometry covers the common needs, so the API stays small:
\itemize{
\item \strong{variable} - \code{"depth"}, \code{"temp"}, or both (\code{c("depth","temp")}) side by side.
\item \strong{group} - split the cohort by a metadata factor (species, sex, ...); each group becomes
a faceted panel of the group mean +/- SE across its individuals. \code{NULL} pools all individuals.
\item \strong{diel} - \code{TRUE} mirrors each bin into night (left) vs day (right) back-to-back bars.
}

Time is weighted by each sample's TRUE duration (not row counts), with long gaps capped, so irregular
or gappy records are not distorted. Depth bins default to a surface-fine, deep-coarse scheme (most
animals spend most time near the surface); every bin is drawn as an equal-height band so all strata
stay legible regardless of width. The look is set by a shared \link{plotTheme} object.
}
\examples{
\dontrun{
files <- list.files("./processed", full.names = TRUE)
plotTimeAtDepth(files, variable = c("depth", "temp"), diel = TRUE)          # ref: pooled mirrored
plotTimeAtDepth(files, group = "species", plot.file = "./plots/tad.pdf")    # faceted by species
}
}
\seealso{
\link{plotTheme}, \link{summarizeTagData}, \link{plotDepthProfiles}, \link{plotDistributions}
}
