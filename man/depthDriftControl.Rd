% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{depthDriftControl}
\alias{depthDriftControl}
\title{Depth zero-offset drift-correction settings for processTagData()}
\usage{
depthDriftControl(
  method = c("surface", "none"),
  surface.evidence = c("dry", "gps"),
  min.dry.duration = 3,
  max.gap = 6,
  min.anchors = 2,
  surface.quantile = 0.05,
  surface.band = 2
)
}
\arguments{
\item{method}{Correction method: \code{"surface"} (surface-anchored zero-offset correction, the default)
or \code{"none"} (disable; depth is left untouched).}

\item{surface.evidence}{Character vector of the evidence sources used to locate surface references,
any of \code{"dry"} (a wet/dry sensor's sustained dry intervals), \code{"gps"} (surface-implying position
fixes - Fastloc-GPS or Argos - whose antenna must break the surface), and \code{"depth"} (an opt-in
"shallow mode" that infers surface intervals from the depth trace itself; see the \verb{surface.*}
arguments). \code{"dry"} and \code{"gps"} are independent of the depth trace and are the safe default;
\code{"depth"} is used only as a gap-filler where the independent sources are absent, and it assumes the
shallowest sustained depth is the surface, so it is unsuitable for animals that rarely surface.
Default uses \code{"dry"} and \code{"gps"}.}

\item{min.dry.duration}{Minimum duration (seconds) of a sustained dry interval for it to count as a
surface anchor; briefer dry flips (spray, wave wash-over) are ignored. Default 3.}

\item{max.gap}{Maximum interval (hours) between consecutive surface anchors for the correction to be
considered fully reliable. Samples inside a longer gap are still corrected (the offset is
interpolated across it) but flagged low-confidence, and the step status becomes \code{"applied_with_gaps"}.
Default 6.}

\item{min.anchors}{Minimum number of surface anchors for a time-varying correction. With exactly one
anchor a single constant offset is applied; with none the correction abstains and depth is left
untouched. Default 2.}

\item{surface.quantile, surface.band}{The surface level is estimated as the \code{surface.quantile} (0.05)
quantile of depth over the deployment (the animal's shallowest sustained depth). Two uses: (1) in
"shallow mode" (\code{surface.evidence = "depth"}), a sample counts as at-surface when its depth is within
\code{surface.band} (2 m) of that estimate; (2) for ALL evidence types, an anchor is a valid zero-offset
only when its depth reads within \code{surface.band} of the surface level - a "surface" fix that lands on a
dive (reading tens of metres) is a mis-timed/mislabelled fix, not the sensor zero drift, and is
rejected (else it would over-correct the depth above the surface). \code{surface.band} should exceed both
the surface wave/noise amplitude and the expected drift magnitude.}
}
\value{
A validated \code{nautilus_depth_drift} object for the \code{depth.drift} argument of \code{\link{processTagData}}.
}
\description{
Bundles the settings for the depth zero-offset drift correction applied by \code{\link{processTagData}}.
Pressure sensors accumulate a slowly-varying zero offset over a deployment (mainly thermal), so an
animal at the surface gradually stops reading 0 m. The correction estimates that offset from
independent surface evidence and subtracts it; by default it never infers the surface from the depth
trace, and it abstains rather than invent a zero line when evidence is too sparse. An opt-in "shallow
mode" (\code{surface.evidence = "depth"}) can additionally infer surface intervals from the depth trace.
}
\examples{
depthDriftControl(surface.evidence = "dry", max.gap = 12)
depthDriftControl(method = "none")   # disable drift correction
}
\seealso{
\code{\link{processTagData}}, \code{\link{smoothingControl}}, \code{\link{calibrationControl}}
}
