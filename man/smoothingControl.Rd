% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{smoothingControl}
\alias{smoothingControl}
\title{Smoothing windows for processTagData()}
\usage{
smoothingControl(static = 3, orientation = 1, dba = 2, depth = 10, speed = 1)
}
\arguments{
\item{static}{Window (s) setting the static/gravity separation: the acceleration is split into a
static (gravity/posture) and a dynamic (motion) part with a zero-phase Butterworth high-pass whose
-3 dB cutoff is the equivalent of this window (~\code{0.76 / static} Hz; the default 3 s gives ~0.25 Hz).
The dynamic part underlies VeDBA/ODBA and surge/sway/heave, the static part the orientation
pitch/roll -- so this is not a cosmetic post-smoother: it defines what counts as "dynamic", must be
\verb{> 0}, and cannot be disabled. Default 3.}

\item{orientation}{Window (s) for orientation metrics (roll, pitch, heading; circular mean). Default 1.}

\item{dba}{Window (s) for VeDBA/ODBA smoothing, applied as a zero-phase Butterworth low-pass whose
-3 dB cutoff is the equivalent of this window (~\code{0.44 / dba} Hz; the default 2 s gives ~0.22 Hz).
\code{NULL} disables the smoothing. Default 2.}

\item{depth}{Window (s) for depth (also drives vertical-speed). Default 10.}

\item{speed}{Window (s) for derived speed/velocity. Default 1.}
}
\value{
A validated \code{nautilus_smoothing} object for the \code{smoothing} argument of \code{\link{processTagData}}.
}
\description{
Groups the smoothing-window arguments of \code{\link{processTagData}} into one object, so the main
call stays uncluttered. Each value is a window length in seconds; set any to \code{NULL} to disable that
smoothing.
}
\examples{
smoothingControl(depth = 15, dba = NULL)   # 15 s depth window; disable DBA post-smoothing
}
\seealso{
\code{\link{processTagData}}, \code{\link{calibrationControl}}
}
